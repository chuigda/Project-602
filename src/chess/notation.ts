// import { ChessGame } from './chessgame';

// export function uci2san(game: ChessGame, validMoves: string[], uci: string): string {
//    const from = uci.slice(0, 2)
//    const to = uci.slice(2, 4)
//    const promotion = uci.slice(4, 5)

//    let piece = ''

// }